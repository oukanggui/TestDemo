<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testdemo">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".stack.StackCardActivity">
        </activity>
        <activity android:name=".floatview.FloatViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".span.DemoActivity"></activity>
        <activity android:name=".viewpager.ViewPagerDemoActivity" />
        <activity android:name=".ring.ProgressCircleImageActivity" />
        <activity android:name=".segment.SegmentTestActivity" />
        <activity android:name=".tab.MainActivity" />
    </application>

</manifest>